---
date: 2026-07-14
---

## VOC 불만 패턴 분석

`raw/data_voc.csv`를 직접 집계·드릴다운함 (voc_type별/category별/월별 분포, `category="해지관련"` & `sentiment="부정"` 95건 원문 전수 확인).

### 채운 노트

- [[q-001-voc-현황]] — "## 분석 결과" 섹션에 집계 표와 sentiment=voc_type 1:1 대응 사실을 채움.

### 생성한 노트

- [[i-002-voc-불만패턴]] — 근거→해석→시사점 구조. 해지관련 불만 95건이 위약금 안내(33건)·이중청구(31건)·번호이동 지연(31건) 3개 정형 문구로만 구성됨을 근거로, 상담 응대보다 처리 프로세스 개선이 필요하다는 시사점 도출. confidence: 높음.

### 연결

- [[q-001-voc-현황]] ↔ [[i-002-voc-불만패턴]] 양방향 위키링크.
- INDEX.md의 `## 인사이트`, `## 최근 로그` 섹션에 추가함.

## SQL 쿼리 폴더 신설

질문 답변용 SQL 쿼리를 별도로 관리하기 위해 `06_sql/` 폴더를 새로 만듦 (raw~05_log에 이어 번호 순서상 다음 폴더).

- `06_sql/README.md` 생성.
- [[sql-q-001-voc-현황]] 생성 — [[q-001-voc-현황]]의 세 집계(voc_type/category/월별)를 재현하는 빅쿼리 쿼리 3종 + 기술 노트(COUNTIF, SAFE_DIVIDE, 윈도우 함수, DATE_TRUNC, CSV BOM 주의사항).
- 파일명은 질문 노트와 동일한 basename 충돌을 피하기 위해 `sql-` 접두사를 붙여 `sql-q-001-voc-현황.md`로 지음 (Obsidian은 위키링크를 파일명 기준으로 해석하므로 basename이 겹치면 링크가 모호해짐).
- [[q-001-voc-현황]]에 SQL 노트로 가는 링크 추가, SQL 노트에도 질문 노트로 가는 역링크 추가.
- INDEX.md에 `## SQL 쿼리` 섹션 신설하고 링크 추가.
- CLAUDE.md 폴더 구조 문서에 `06_sql/` 설명 추가.

## 저만족도 원인 분석 (channel 요인 확인)

`data_satisfaction`을 `consult_id`로 `data_consultations`와, `customer_id`로 `data_customers`와 조인해 category/channel/plan/region 4개 세그먼트와 duration_min/agent_id 2개 가설을 확인함.

### 채운 노트

- [[q-004-저만족도-원인]] — "## 분석 결과"에 세그먼트별 CSAT·NPS 표, channel×재문의율 상관(-0.954), duration_min·agent_id 가설 기각 과정을 채움.

### 생성한 노트

- [[i-003-저만족도-채널요인]] — 근거→해석→시사점 구조. category/plan/region/agent_id는 편차가 작고(0.15~0.54점) channel만 편차가 큼(1.19점)을 근거로, 저만족도의 주원인이 channel(특히 이메일)이라는 결론 도출. confidence: 높음.

### 연결

- [[q-004-저만족도-원인]] ↔ [[i-003-저만족도-채널요인]] 양방향 위키링크.
- [[i-001-채널별-재문의율]] ↔ [[i-003-저만족도-채널요인]] 양방향 위키링크 (같은 채널 순위를 재문의율·CSAT 두 지표로 교차 확인).
- INDEX.md의 `## 인사이트` 섹션에 추가함.
