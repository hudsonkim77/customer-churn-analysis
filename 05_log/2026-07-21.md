---
date: 2026-07-21
---

## 빅쿼리 MCP 연결 확인

`project1_day1` 데이터셋 확인. 기존 raw/ 5종과 동일한 이름의 테이블(customers/consultations/satisfaction/usage_history/voc, 2026-06-15 생성) 외에 `agents` 테이블이 새로 있음(2026-07-21 생성).

## agents 테이블 반영

BigQuery `project1_day1.agents`(20행, `raw/`에는 대응 파일 없음)을 조회해 스키마 노트로 반영함.

### 생성한 노트

- [[agents]] — `agent_id`(PK), `team`, `hire_date`, `employment_type`, `monthly_call_target`, `training_completed_yn`, `qa_score`, `overtime_hours_avg`, `agent_satisfaction`, `turnover_yn` 10개 컬럼. `agent_id` 20개(AG01~AG20)가 [[data_consultations]]의 `agent_id`와 정확히 일치함을 확인함.

### 연결

- [[data_consultations]]의 `agent_id` 컬럼 설명에 [[agents]] 링크 추가, "연결" 섹션에 `agent_id` 기준 연결 추가.
- [[i-003-저만족도-채널요인]]의 "agent_id 가설" 후속 질문 항목에 [[agents]] 링크 추가 — team/qa_score/overtime_hours_avg/turnover_yn로 상담원×채널 교차분석을 더 정교하게 할 수 있는 데이터가 확보됐음을 기록함.
- INDEX.md `## 데이터` 섹션에 [[agents]] 추가함.

### 비고

- `qa_score`, `agent_satisfaction`의 만점 기준은 BigQuery 컬럼 설명이 비어 있어 불명확 — 값 범위(qa_score 3.0~4.6, agent_satisfaction 2~9)로만 추정해 노트에 명시함.
